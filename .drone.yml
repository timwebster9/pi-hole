kind: pipeline
name: default

steps:
- name: build
  image: timwebster9/build
  commands:
  - cd neptune/nginx
  - make push
  volumes:
  - name: docker
    path: /var/run/docker.sock

volumes:
- name: docker
  host:
    path: /var/run/docker.sock